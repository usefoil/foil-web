{
  "name": "foil-web",
  "version": "0.1.0",
  "private": true,
  "description": "Static landing page and SEO content for Foil.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:analytics": "node scripts/check-analytics.mjs",
    "check:observability": "node scripts/check-observability.mjs",
    "check:launch": "npm run build && node scripts/check-launch.mjs && npm run check:analytics && npm run check:observability",
    "serve": "python3 -m http.server 4173"
  },
  "engines": {
    "node": ">=20"
  }
}
